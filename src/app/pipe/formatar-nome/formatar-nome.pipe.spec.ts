import { FormatarNomePipe } from './formatar-nome.pipe';

describe('FormatarNomePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatarNomePipe();
    expect(pipe).toBeTruthy();
  });
});
